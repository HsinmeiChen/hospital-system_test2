# UV 使用教學

本文件提供團隊成員日常使用 `uv` 管理 Python／Django 專案時需要的基本觀念、常用指令與操作原則。

> 本文件著重於「如何使用 uv」。
>
> 實際操作紀錄請參閱：
>
- `01.UV_MIGRATION.md`：記錄原始 Python 3.8 Django 專案由 `requirements.txt` 遷移至 uv 的流程
- `02.UV_PROJECT_SETUP.md`：記錄模擬同事從 Git Clone 專案後，建立本機開發環境的流程
- `03.UV_PROJECT_UPGRADE.md`：記錄專案套件整理，以及 Python／Django 版本升級流程

---

## 1. 為什麼使用 uv

原本 Python 專案常見的套件管理方式為：

```text
requirements.txt
+
pip
+
venv
```

導入 uv 後，主要改為：

```text
pyproject.toml
+
uv.lock
+
.venv
```

uv 可以協助：

- 管理 Python 版本
- 建立虛擬環境
- 管理專案依賴
- 安裝與同步套件
- 在專案環境中執行 Python 程式或其他命令
- 透過 `uv.lock` 記錄並鎖定依賴解析結果

---

## 2. 第一次 Clone 專案後怎麼做

專案已經完成 uv 初始化，因此一般開發者 Clone 專案後：

**不需要重新執行：**

```powershell
uv init
```

也不需要重新執行：

```powershell
uv add -r requirements.txt
```

### 2.1 Clone 專案

```powershell
git clone <專案 Git URL>
cd <專案目錄>
```

### 2.2 建立並同步專案環境

```powershell
uv sync --locked
```

此指令會依照既有的 `pyproject.toml` 與 `uv.lock`：

1. 確認 `uv.lock` 與專案設定一致。
2. 在 `.venv` 不存在時建立虛擬環境。
3. 依照 `uv.lock` 安裝鎖定的套件版本。
4. 若 `uv.lock` 與專案設定不一致，停止執行而不自動修改鎖定檔。

### 2.3 補上專案需要的環境與資料

依專案規範補上：

```text
.env
media/
Public/common/doc/
Public/common/img/
```

`.env` 不提交 Git。

### 2.4 檢查 Django 專案

```powershell
uv run --locked python manage.py check
```

正常情況會顯示類似：

```text
System check identified no issues
```

### 2.5 啟動 Django

```powershell
uv run --locked python manage.py runserver
```

---

## 3. 日常套件管理

### 3.1 新增正式依賴

如果程式正式需要新的套件：

```powershell
uv add <PACKAGE>
```

例如：

```powershell
uv add requests
```

`uv add` 會同步更新：

```text
pyproject.toml
uv.lock
.venv
```

因此不要只使用：

```powershell
pip install <PACKAGE>
```

如果套件是專案正式依賴，應使用 `uv add`，讓其他人可以透過 Git 與 `uv.lock` 重建相同環境。

### 3.2 新增開發依賴

如果套件只用於開發、檢查或測試：

```powershell
uv add --dev <PACKAGE>
```

例如：

```powershell
uv add --dev deptry
```

### 3.3 移除套件

```powershell
uv remove <PACKAGE>
```

例如：

```powershell
uv remove xlrd
```

移除後，uv 會同步更新專案依賴設定與 lockfile。

### 3.4 查看目前已安裝套件

```powershell
uv pip list
```

此指令主要用來查看目前環境中已安裝的套件。

正式新增依賴仍應使用：

```powershell
uv add <PACKAGE>
```

### 3.5 查看依賴樹

```powershell
uv tree
```

可以查看：

- 專案的直接依賴
- 各套件帶入的間接依賴
- 套件之間的相依關係

---

## 4. 認識 uv 專案的重要檔案

### 4.1 `pyproject.toml`

`pyproject.toml` 是 Python 專案的主要設定檔。

uv 會從中讀取：

- 專案名稱與版本
- 支援的 Python 版本範圍
- 正式依賴
- 開發依賴
- uv 或其他 Python 工具設定

可以簡單理解為：

```text
pyproject.toml = 專案需要什麼
```

### 4.2 `uv.lock`

`uv.lock` 是 uv 產生的鎖定檔（lockfile），應提交 Git。

它記錄：

- 直接依賴
- 間接依賴
- uv 實際解析後採用的版本
- 不同平台或 Python 條件下的依賴資訊

可以簡單理解為：

```text
uv.lock = 記錄並鎖定專案使用的套件版本
```

不建議手動修改，也不要因為同步失敗就直接刪除。

### 4.3 `.python-version`

`.python-version` 記錄此專案偏好使用的 Python 版本，例如：
```text
3.8
```

或專案升級後：
```text
3.12
```

### 4.4 `.venv/`

`.venv/` 是專案本機的虛擬環境，用來存放此專案環境中安裝的套件與執行環境相關檔案。

`.venv/` 不提交 Git。

如果需要重新建立：

```powershell
uv sync --locked
```

### 4.5 `.env`

`.env` 不提交 Git。

---

## 5. 直接依賴與間接依賴

### 5.1 直接依賴

直接依賴是專案明確宣告需要的套件。

例如：

```text
專案
└── Django
```

如果專案在 `pyproject.toml` 中明確宣告需要 Django，Django 就是直接依賴。

直接依賴通常會記錄在：

```text
pyproject.toml
```

### 5.2 間接依賴

間接依賴是直接依賴為了正常運作而需要的其他套件。

例如：

```text
專案
└── Django
    ├── asgiref
    └── sqlparse
```

其中：

```text
Django → 直接依賴

asgiref → 間接依賴
sqlparse → 間接依賴
```

一般情況下，不需要手動把所有間接依賴都加入 `pyproject.toml`。

uv 會在依賴解析時一併處理，並將解析結果記錄到 `uv.lock`。

可以透過：

```powershell
uv tree
```

查看完整依賴關係。

---

## 6. Sync 與 `uv run`

### 6.1 `uv sync`

```powershell
uv sync
```

`uv sync` 會讓 `.venv` 與目前的專案依賴設定保持一致。

如果 `uv.lock` 需要更新，uv 可能會先更新 `uv.lock`，再同步 `.venv`。

### 6.2 `uv sync --locked`

```powershell
uv sync --locked
```

`uv sync --locked` 同樣會同步 `.venv`，但要求現有的 `uv.lock` 必須與專案設定一致。

如果 `uv.lock` 需要更新，指令會停止執行，不會自動修改 `uv.lock`。

### 6.3 使用 `uv run` 

`uv run` 會在專案環境中執行指定的命令，不需要先手動啟用 `.venv`。
Django 專案可透過 `python manage.py ...` 執行管理指令。

一般執行方式：

```powershell
uv run python manage.py check
```

```powershell
uv run python manage.py runserver
```

使用 `--locked`：

```powershell
uv run --locked python manage.py check
```

```powershell
uv run --locked python manage.py runserver
```

兩者差異：

- `uv run`：在執行命令前會確認專案環境，必要時可能更新 `uv.lock` 或同步 `.venv`。
- `uv run --locked`：要求既有 `uv.lock` 必須與專案設定一致；若 lockfile 需要更新，則停止執行，不會自動修改 `uv.lock`。

若目的是依照 Git 中既有的 lockfile 執行專案，建議使用：
```powershell
uv run --locked ...
```

---

## 7. 團隊使用 uv 的基本原則

### 原則 1：正式依賴使用 `uv add`

如果專案需要新套件：
```powershell
uv add <PACKAGE>
```

### 原則 2：`.venv` 不提交 Git

### 原則 3：`uv.lock` 要提交 Git

### 原則 4：不要隨意刪除 `uv.lock`

`uv.lock` 是團隊重建一致環境的重要依據。

如果同步失敗，不能直接刪除 lockfile。


---

## 8. 常用指令速查

| 目的 | 指令 |
|---|---|
| 查看 uv 版本 | `uv --version` |
| Clone 後建立環境 | `uv sync --locked` |
| 同步環境並允許更新 lockfile | `uv sync` |
| 執行 Django system check | `uv run --locked python manage.py check` |
| 啟動 Django | `uv run --locked python manage.py runserver` |
| 新增正式依賴 | `uv add <PACKAGE>` |
| 新增開發依賴 | `uv add --dev <PACKAGE>` |
| 移除依賴 | `uv remove <PACKAGE>` |
| 查看已安裝套件 | `uv pip list` |
| 查看依賴樹 | `uv tree` |
| 查看 uv cache 位置 | `uv cache dir` |

---