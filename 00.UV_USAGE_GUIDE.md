# UV 使用教學

本文件提供團隊成員日常使用 `uv` 管理 Python／Django 專案時需要的基本觀念、常用指令與操作原則。

> 本文件著重於「如何使用 uv」。
>
> 實際操作紀錄請參閱：
>
- `01.UV_MIGRATION.md`：記錄原始 Python 3.8 Django 專案由 `requirements.txt` 遷移至 uv 的流程
- `02.UV_PROJECT_SETUP.md`：記錄模擬同事從 Git Clone 專案後，建立本機開發環境的流程
- `03.UV_PROJECT_UPGRADE.md`：記錄專案套件整理，以及 Python／Django 版本升級流程

---

## 1. 為什麼使用 uv

原本的套件管理與導入 `uv` 後的轉變：

| 管理項目 | 傳統做法 | 導入 uv 後 |
| :--- | :--- | :--- |
| **依賴與設定** | `requirements.txt` | `pyproject.toml` |
| **版本鎖定** | *無（或使用 pip-tools）* | `uv.lock` |
| **套件與環境管理** | `pip` + `venv` | `.venv` (由 `uv` 統一管理) |

`uv` 可協助管理 Python 版本、虛擬環境、專案依賴，以及套件安裝與執行環境。

---

## 2. 第一次 Clone 專案

### 2.1 Clone 專案

```powershell
git clone <專案 Git URL>
cd <專案目錄>
```

### 2.2 建立並同步專案環境

```powershell
uv sync --locked
```

`uv sync --locked` 會依照既有的 `pyproject.toml` 與 `uv.lock` 建立或同步 `.venv`。
若 `uv.lock` 與專案設定不一致，指令會停止且不修改 lockfile。


### 2.3 檢查 Django 專案

```powershell
uv run --locked python manage.py check
```

正常情況會顯示類似：System check identified no issues

### 2.4 啟動 Django

```powershell
uv run --locked python manage.py runserver
```

---

## 3. 日常套件管理

### 3.1 新增正式依賴

如果程式正式需要新的套件：

```powershell
uv add <PACKAGE>
```

例如：

```powershell
uv add requests
```

如果套件是專案正式依賴，應使用 `uv add`，讓其他人可以透過 Git 與 `uv.lock` 重建相同環境。

### 3.2 新增開發依賴

如果套件只用於開發、檢查或測試：

```powershell
uv add --dev <PACKAGE>
```

例如：

```powershell
uv add --dev deptry
```

### 3.3 移除套件

```powershell
uv remove <PACKAGE>
```

例如：

```powershell
uv remove xlrd
```

移除後，uv 會同步更新專案依賴設定與 lockfile。

### 3.4 查看目前已安裝套件

```powershell
uv pip list
```

`uv pip list` 用於查看目前環境已安裝的套件；其中 `uv pip` 是 uv 提供的 pip 相容介面。


### 3.5 查看依賴樹

```powershell
uv tree
```

例如：

```text
專案
└── Django → 直接依賴
    ├── asgiref → 間接依賴
    └── sqlparse → 間接依賴
```
直接依賴是專案明確宣告需要的套件，間接依賴是直接依賴為了正常運作而需要的其他套件。

---

## 4. uv 專案的重要檔案

| 檔案 | 用途 | 是否提交 Git |
|---|---|---|
| `pyproject.toml` | 記錄專案設定與依賴 | 是 |
| `uv.lock` | 記錄並鎖定專案使用的套件版本 | 是 |
| `.python-version` | 指定專案使用的 Python 版本 | 是 |
| `.venv/` | 本機虛擬環境 | 否 |
| `.env` | 專案環境變數 | 否 |

> `uv.lock` 不建議手動修改，也不要因為同步失敗就直接刪除。

---

## 5. `Sync` 與 `uv run`

### 5.1 `uv sync`

同步專案環境；若 uv.lock 需要更新，uv 可能會先更新 lockfile。

```powershell
uv sync
```

### 5.2 使用 `uv sync --locked` 

依既有的`uv.lock`同步環境；若`lockfile`與專案設定不一致，停止執行且不修改`uv.lock`。

```powershell
uv sync --locked
```

### 5.3 使用 `uv run` 

`uv run` 會在專案環境中執行指定的命令，不需要先手動啟用 `.venv`。

```powershell
uv run python manage.py check
```

```powershell
uv run python manage.py runserver
```

使用 `--locked`：

```powershell
uv run --locked python manage.py check
```

```powershell
uv run --locked python manage.py runserver
```

兩者差異：
- `uv run`：在執行命令前會確認專案環境，必要時可能更新 `uv.lock` 或同步 `.venv`。
- `uv run --locked`：要求既有 `uv.lock` 必須與專案設定一致；若 lockfile 需要更新，則停止執行，不會自動修改 `uv.lock`。

---

## 6. 常用指令速查

| 目的 | 指令 |
|---|---|
| 查看 uv 版本 | `uv --version` |
| Clone 後建立環境 | `uv sync --locked` |
| 同步環境並允許更新 lockfile | `uv sync` |
| 執行 Django system check | `uv run --locked python manage.py check` |
| 啟動 Django | `uv run --locked python manage.py runserver` |
| 新增正式依賴 | `uv add <PACKAGE>` |
| 新增開發依賴 | `uv add --dev <PACKAGE>` |
| 移除依賴 | `uv remove <PACKAGE>` |
| 查看已安裝套件 | `uv pip list` |
| 查看依賴樹 | `uv tree` |

---