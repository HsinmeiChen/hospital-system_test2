# 「意見反映」表單寄信功能實施計畫

## 目標說明

將現有的「意見反映」頁面（`Contact_us.html`）從靜態表單，升級為完整的動態寄信功能。
參考 `specialty_health` 及 `Breast_Care_Center` 已有的寄信架構，沿用相同的表單驗證、驗證碼、HTML 郵件模板機制，並針對主站（Pomelo_API）場景補齊所有資安設定。

---

## 現況分析（基於最新 views.py 重新精確掃描）

> [!IMPORTANT]
> 舊計畫分析的是備份 `views2.py`（含硬編碼憑證的舊版）。使用者已大幅重構 `views.py`：移除硬編碼、改用 settings、加入 oracledb Thick Mode、SQL 注入防護等。以下為基於**最新版**的正確分析。

| 項目 | 現況 |
|---|---|
| 主程式 | **`Pomelo_API/views.py`**（5110 行），`urls.py` import 此檔 |
| 舊版備份 | `Pomelo_API/views2.py`（4602 行）— 含硬編碼憑證，**不使用** |
| 表單 action | `Contact_us.html` Line 28：`AutoGenerated/WEBcomplain.asp`（舊 ASP，無效） |
| 後端 view | `views.py` **Line 3558**：`def A004_contact_us(request)` — 只有 `render()`，**無 POST 處理** |
| URL 已註冊 | `urls.py` **Line 128**：`re_path('A004_contact_us/', pomelo_views.A004_contact_us)` ✅ 已存在 |
| Email 設定 | `settings.py` Lines 279-311：`EMAIL_BACKEND=smtp`、`EMAIL_HOST/USER/PWD` 完整，`EmailBackend` ✅ 已啟用 |
| 收件者解析 | `settings.py` Line 301：`_parse_contact_recipients()` 已定義 EECP/HEALTH/BREAST，**主站版本尚缺** |
| 寄信模組 | `specialty_health/forms.py`：`ContactForm` + `send_email_to_client()` 為完整移植參考 |
| Rate limit | `decorators.py` Line 28：`ratelimit_form_submit` ✅ 可用 |
| 驗證碼鎖定 | `decorators.py` Line 49：`captcha_failure_limit` ✅ 可用 |
| 驗證碼刷新限制 | `decorators.py` Line 6：`ratelimit_captcha` ✅ 可用 |
| 驗證碼生成 | 需確認 `utils.py` 是否有 `generate_captcha_image_bytes()`（前次分析有，需再驗） |

---

## 需補強的資安項目（Task 3 清單）

| 編號 | 問題 | 解決方案 |
|---|---|---|
| S-1 | **無 CSRF 保護** — 表單原 action 為外部 ASP | Django `{% csrf_token %}` 已由 middleware 覆蓋；新表單需正確加入 token |
| S-2 | **無驗證碼** — 表單可被 Bot 大量提交 | 整合現有 Session-based 數字圖形驗證碼（與 Breast/Health 一致）|
| S-3 | **無 Rate Limiting** — 同一 IP 可無限次送出 | 使用 `ratelimit_form_submit` 裝飾器（5 分鐘內最多 5 次） |
| S-4 | **驗證碼失敗無鎖定** — 可暴力破解驗證碼 | 使用 `captcha_failure_limit` 裝飾器（5 次失敗後鎖定 5 分鐘） |
| S-5 | **無輸入長度/格式驗證** — 使用者可送出任意長度字串 | Django `Form` 欄位加上 `max_length`、`validators`（電話格式） |
| S-6 | **無 XSS 過濾** — 使用者輸入被直接放入信件 | `django.utils.html.escape()` 或 Django Template auto-escape |
| S-7 | **Content-Type 未限制** — POST body 可為任意類型 | View 只接受 `application/x-www-form-urlencoded` / `multipart`（Django 表單預設） |
| S-8 | **驗證碼無有效期限** — 驗證碼可被暫存長期使用 | 驗證碼加上 `captcha_timestamp`，120 秒後失效 |
| S-9 | **收件信箱硬編碼** | 透過 `.env` + `settings.py` 讀取 `CONTACT_EMAIL_RECIPIENTS_MAIN` |
| S-10 | **信件內容缺乏排版** | 使用 HTML Email Template，結構化顯示所有欄位 |

---

## 表單欄位規劃

依據現有 `Contact_us.html`，將舊表單的欄位對應至 Django Form：

| 欄位 | HTML name | Form 欄位 | 驗證 |
|---|---|---|---|
| 反應事件類別 | `optradio` | `ChoiceField` | 必填，選項：讚美/申訴/建議/諮詢 |
| 姓名 | `name_in` | `CharField` | 必填，max 50 |
| 聯絡電話 | `phone_in` | `CharField` | 選填，電話格式 |
| 事件發生日期 | `date_in` | `DateField` | 選填 |
| 事件發生時間 | `time_in` | `TimeField` | 選填 |
| 發生地點 | `place_in` | `CharField` | 選填，max 100 |
| 反應內容 | `message` | `CharField(Textarea)` | 必填，max 500（原 150 字太少） |
| 驗證碼 | `captcha` | `CharField` | 必填，5 位數 |

> **注意：** 原表單未要求 Email，本計畫**新增 `email` 欄位（選填）**，供客服回覆用；若不需要可移除。

---

## 施作計畫（依修改檔案分組）

---

### 1. settings.py

#### [MODIFY] [settings.py](file:///c:/everan_website_t/Pomelo_test/Pomelo_test/settings.py)

在 `_parse_contact_recipients` 區塊末尾新增主站意見反映專用收件者：

```python
# 主站意見反映頁 收件信箱
CONTACT_EMAIL_RECIPIENTS_MAIN = _parse_contact_recipients('CONTACT_EMAIL_RECIPIENTS_MAIN', NOTIFY_EMAIL_RECIPIENTS)
```

---

### 2. .env

#### [MODIFY] [.env](file:///c:/everan_website_t/Pomelo_test/.env)

新增：
```
CONTACT_EMAIL_RECIPIENTS_MAIN=customer_service@everanhospital.com.tw
```

---

### 3. Pomelo_API/forms.py（新建）

#### [NEW] [forms.py](file:///c:/everan_website_t/Pomelo_test/Pomelo_API/forms.py)

建立主站意見反映 Form（`ContactUsForm`）+ 寄信函式（`send_contact_us_email`）：

- **欄位**：`category`, `name`, `email`(選填), `phone`(選填), `incident_date`(選填), `incident_time`(選填), `place`(選填), `message`, `captcha`
- **寄信**：使用 `EmailMessage`，HTML body 透過 Django Template 渲染
- **收件者**：`settings.CONTACT_EMAIL_RECIPIENTS_MAIN`

---

### 4. templates/email/contact_us_email.html（新建）

#### [NEW] [contact_us_email.html](file:///c:/everan_website_t/Pomelo_test/templates/email/contact_us_email.html)

HTML Email 模板，以表格排版顯示所有收集到的欄位資訊，供客服閱讀。

---

### 5. Pomelo_API/views.py（修改 A004_contact_us）

#### [MODIFY] [views.py](file:///c:/everan_website_t/Pomelo_test/Pomelo_API/views.py)

修改 Line 3558 的 `A004_contact_us` view：
- 改為處理 GET（顯示表單）與 POST（驗證並寄信）
- 整合驗證碼 session 機制
- 套用 `@ratelimit_form_submit` + `@captcha_failure_limit` 裝飾器（從 `Pomelo_test.decorators` 匯入）
- 成功後使用 PRG 模式（`redirect`）防止 F5 重複提交

新增驗證碼圖片 API（在同一檔案加入）：
- `A004_contact_captcha_img`：回傳驗證碼 PNG 圖片
- `A004_refresh_captcha`：AJAX 刷新驗證碼

---

### 6. Pomelo_test/urls.py（修改）

#### [MODIFY] [urls.py](file:///c:/everan_website_t/Pomelo_test/Pomelo_test/urls.py)

新增三個 URL：
```python
re_path('A004_contact_us/', pomelo_views.A004_contact_us),         # 已存在
re_path('A004_contact_captcha_img/', pomelo_views.A004_contact_captcha_img),
re_path('A004_refresh_captcha/', pomelo_views.A004_refresh_captcha),
```

---

### 7. templates/Contact_us.html（修改）

#### [MODIFY] [Contact_us.html](file:///c:/everan_website_t/Pomelo_test/templates/Contact_us.html)

- 修改 `<form action>` 指向正確的 Django URL
- 加入 `{% csrf_token %}`
- 加入驗證碼圖片區塊
- 更新欄位 name 對應 Form 欄位
- 新增成功/失敗訊息提示（使用 Django `messages` 框架）
- 新增 `email` 選填欄位
- JS：驗證碼 AJAX 刷新功能

---

## 開放問題

> [!IMPORTANT]
> **Q1：意見反映信箱** — 請確認收件信箱應設為哪個地址（如：客服信箱/品質管理室）。目前計畫中以 `CONTACT_EMAIL_RECIPIENTS_MAIN` 在 `.env` 中設定。

> [!IMPORTANT]
> **Q2：Email 欄位是否必要** — 原表單無 Email 欄位。若客服需要能主動 Reply，建議新增選填 Email 欄位。是否加入？

> [!NOTE]
> **Q3：字數上限** — 原 `message` 欄位限 150 字，建議放寬至 500 字（方便使用者詳述事件）。是否同意？

> [!NOTE]
> **Q4：裝飾器引用路徑** — `Pomelo_API/views1.py` 需從 `Pomelo_test.decorators` 匯入裝飾器；目前 views1.py 沒有使用 decorators，需確認可匯入（路徑已存在，應無問題）。

---

## 驗證計畫

### 自動驗證
- 啟動 `python manage.py runserver` 確認無 ImportError
- 訪問 `http://127.0.0.1:xxxx/A004_contact_us/` 確認頁面正常顯示
- 送出空白表單確認驗證錯誤訊息出現
- 送出正確表單確認信件寄出（開發環境 EMAIL_BACKEND 使用 console 輸出）

### 人工驗證
- 確認驗證碼圖片能正常顯示並可刷新
- 確認 Rate Limit 在超過 5 次後返回錯誤
- 確認客服收到的信件排版清晰
