
# GitHub 專案複製與本地開發環境建置流程

說明如何從 GitHub 複製（Clone）專案至本地端，並完成環境設定、套件同步、靜態資源配置與服務啟動。

## 一、 複製專案（Clone Repository）

開啟 PowerShell，執行以下指令將專案複製至本地端並進入專案目錄：

```bash
# 1. 複製專案
git clone <專案的 GitHub URL>

# 2. 切換至專案根目錄
cd <專案目錄名稱>
```

---

## 二、 安裝與同步依賴套件

使用 `uv` 自動建立虛擬環境並安裝所有依賴套件：

```bash
uv sync --locked
```

---

## 三、 設定環境變數與靜態資源目錄

在專案根目錄下，請確保補齊以下必要的環境變數檔案與實體檔案目錄：

### 1. 新增環境變數檔
在專案根目錄新增 `.env` 檔案，並填入所需的環境變數設定。

### 2. 補齊媒體與資源目錄
請確保專案目錄中包含以下資料夾與檔案結構（到 66.164 複製）：

* 📁 `Pomelo_test/media/`（媒體檔案儲存目錄）
* 📁 `Pomelo_test/Public/common/doc/`
* 📁 `Pomelo_test/Public/common/img/`

---

## 四、 驗證與資料庫遷移

### 1. 執行系統檢查
```bash
uv run python manage.py check
```

> [!NOTE]
> 執行 `uv run python manage.py check` 若完全正常，終端機將會顯示：
> `System check identified no issues (0 silenced).`

### 2. 執行資料庫 Migration

```bash
uv run python manage.py migrate
```

---

## 五、 啟動開發伺服器（Run Server）

完成上述設定與資料庫遷移後，即可啟動本地開發伺服器：

```bash
uv run python manage.py runserver
```


