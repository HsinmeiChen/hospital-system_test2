# 使用 uv 遷移與管理 Python／Django 專案

本文件說明如何將既有、以 `requirements.txt` 管理套件的 Django 專案遷移至 uv。

指定使用：

```text
uv 0.10.12
Python 3.8.x
```

## 目錄

1. [進入 Django 專案根目錄](#1-進入-django-專案根目錄)
2. [安裝指定版本的 uv](#2-安裝指定版本的-uv)
3. [清除舊虛擬環境](#3-清除舊虛擬環境)
4. [初始化 uv 專案](#4-初始化-uv-專案)
5. [從 requirements.txt 匯入](#5-從-requirementstxt-匯入)
6. [使用 uv sync 還原與同步環境](#6-使用-uv-sync-還原與同步環境)
7. [檢查 Django 專案設定](#7-檢查-django-專案設定)
8. [提交遷移結果](#8-提交遷移結果)
9. [遷移後的日常開發指令](#9-遷移後的日常開發指令)
10. [認識 uv 專案的重要檔案](#10-認識-uv-專案的重要檔案)
11. [理解 uv 的核心概念](#11-理解-uv-的核心概念)
12. [常用指令速查](#12-常用指令速查)

---

## 1. 進入 Django 專案根目錄

請開啟 PowerShell，將 `<POMELO_TEST_PATH>` 替換成自己電腦中 `Pomelo_test` 資料夾的完整路徑：

```powershell
cd '<POMELO_TEST_PATH>'
```

例如：

```powershell
cd 'C:\Users\<USERNAME>\Desktop\hospital-system\Pomelo_test'
```


簡單確認目前目錄中存在 Django 與原有套件清單：

```powershell
Test-Path '.\manage.py'
Test-Path '.\requirements.txt'
```

預期結果皆為：

```text
True
```

後續指令都必須在此目錄中執行。

---

## 2. 安裝指定版本的 uv

本專案統一使用 uv `0.10.12`。請在 PowerShell 執行：

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/0.10.12/install.ps1 | iex"
```

安裝完成後，請重新開啟 PowerShell 或 VS Code Terminal，再確認版本：

```powershell
uv --version
```

預期結果：

```text
uv 0.10.12
```

若仍顯示找不到 `uv`，請先重新開啟終端機，再確認 uv 的安裝目錄是否已加入 Windows `PATH`。

> 本專案指定使用 uv `0.10.12`。請勿自行執行 `uv self update`，以免與團隊使用的版本不同。

---

## 3. 清除舊虛擬環境

確認目前位於 `Pomelo_test` 目錄後，移除舊的虛擬環境。若目錄不存在，PowerShell 會自動跳過：

```powershell
Remove-Item -Recurse -Force '.\.venv' -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force '.\venv' -ErrorAction SilentlyContinue
```

此步驟只會移除：

```text
.venv/
venv/
```


---

## 4. 初始化 uv 專案

一般的 `uv init` 是建立新應用程式的指令。視現有目錄內容及 uv 版本，它還可能建立 README、範例 Python 檔案及 Git 相關檔案。

若為既有 Django 專案，建議使用最小初始化：

```powershell
uv init --bare --python 3.8
uv python pin 3.8
```

執行後會產出以下兩個檔案：

```text
pyproject.toml
.python-version
```

其中：

- `uv init --bare --python 3.8`：建立最小的 `pyproject.toml`，並將 Python 需求設定為 3.8。
- `uv python pin 3.8`：建立 `.python-version`，讓 uv 在此專案中優先使用 Python 3.8。


> 如果 `pyproject.toml` 已經存在，`uv init` 會停止。此時應先確認專案是否已完成 uv 初始化，而不是刪除原有檔案後重新開始。

---

## 5. 從 `requirements.txt` 匯入

執行：

```powershell
uv add -r requirements.txt
```

uv 會將 `requirements.txt` 中列出的套件加入 `pyproject.toml`，解析完整依賴後產生 `uv.lock`，並自動建立或同步 `.venv`。

執行後的主要產物：

- `pyproject.toml`：加入從 `requirements.txt` 匯入的套件與版本限制。
- `uv.lock`：記錄 uv 解析後的完整依賴與實際鎖定版本。
- `.venv/`：專案使用的本機虛擬環境，套件會安裝在此目錄中。


> **註記：`uv add` 已經同步環境**  
> 執行 `uv add -r requirements.txt` 時，uv 會更新專案依賴、產生 `uv.lock`，並建立或同步 `.venv`，因此不需要再用其他安裝指令重複安裝相同套件。

> **註記：暫時保留 `requirements.txt`**  
> 完成 uv 遷移後，暫時不要刪除 `requirements.txt`。應先確認既有部署流程、CI/CD、伺服器或其他同事的安裝方式是否仍依賴此檔案。


---

## 6. 使用 `uv sync` 還原與同步環境

執行 `uv add -r requirements.txt` 時，uv 已經完成以下工作：

- 更新 `pyproject.toml`。
- 產生 `uv.lock`。
- 建立或同步 `.venv`。
- 將套件安裝至 `.venv`。

因此，第一次執行 `uv add -r requirements.txt` 後，不需要為了安裝相同套件再次執行 `uv sync`。

### 6.1 推薦使用：`uv sync --locked`

當同仁 Clone 專案，或需要重新建立 `.venv` 時，建議執行：

```powershell
uv sync --locked
```

這個指令會根據專案既有的 `pyproject.toml` 與 `uv.lock`：

1. 確認 `uv.lock` 與 `pyproject.toml` 一致。
2. 在 `.venv` 不存在時自動建立虛擬環境。
3. 依照 `uv.lock` 安裝鎖定的套件版本。
4. 如果 `uv.lock` 已過期或與專案設定不一致，停止執行而不自動修改鎖定檔。



### 6.2 其他 `uv sync` 用法

一般同步：

```powershell
uv sync
```

`uv sync` 會同步 `.venv`，必要時可能更新 `uv.lock`。它較適合由專案維護者在調整依賴後使用。

只預覽同步結果：

```powershell
uv sync --locked --dry-run
```

這個指令只會顯示預計進行的環境變更，不會修改 `uv.lock` 或 `.venv`。

若目的是建立或還原專案環境，直接使用：

```powershell
uv sync --locked
```

---

## 7. 檢查 Django 專案設定

使用 uv 管理的環境執行 Django system check：

```powershell
uv run --locked python manage.py check
```

正常情況會顯示類似：

```text
System check identified no issues
```

這代表 Django 專案設定、已安裝 App、模型與部分系統組態通過檢查。

此步驟只檢查 Django 專案設定。

---

## 8. 提交遷移結果

請確保 .gitignore 包含以下設定：

# 排除虛擬環境與環境變數檔
.venv/
venv/
.env

# 務必追蹤（不要排除）：
# pyproject.toml
# uv.lock
# .python-version

`requirements.txt` 是否保留在 Git 中，應等確認部署、CI/CD 與其他既有流程後再決定。遷移當下不要直接刪除。

---

## 9. 遷移後的日常開發指令

以下指令都應在 `Pomelo_test` 目錄中執行。

### 9.1 啟動 Django 本機伺服器

```powershell
uv run python manage.py runserver
```

### 9.2 新增正式依賴

```powershell
uv add <PACKAGE>
```

例如：

```powershell
uv add requests
```

`uv add` 會同時更新 `pyproject.toml`、`uv.lock` 與 `.venv`。


### 9.3 移除套件

```powershell
uv remove <PACKAGE>
```

### 9.4 同事 Clone 專案後還原環境

```powershell
uv sync --locked
```

### 9.5 查看已安裝套件

```powershell
uv pip list
```

### 9.6 查看依賴樹

```powershell
uv tree
```

不要只使用以下指令修補自己的環境：

```powershell
pip install <PACKAGE>
```

如果程式正式需要某個套件，應使用 `uv add`，讓依賴變更能被 Git 記錄並由其他同事重建。

---

## 10. 認識 uv 專案的重要檔案

### 10.1 `requirements.txt`：原有的套件紀錄

`requirements.txt` 是專案原本使用 pip 工作流程時的套件清單。內容可能是：

- 人工維護的直接依賴。
- 帶有版本限制的依賴。
- 由 `pip freeze` 產生的完整環境套件列表。

導入 uv 後，`requirements.txt` 不再是 uv 專案的主要依賴來源，但在確認舊部署流程全部完成遷移前，仍應暫時保留。

### 10.2 `pyproject.toml`：宣告專案需要什麼

`pyproject.toml` 是 Python 專案的主要設定與中繼資料檔。uv 會從中讀取：

- 專案名稱與版本。
- 允許的 Python 版本範圍。
- 程式執行所需的直接依賴。
- 開發、測試或其他 dependency groups。
- uv 與其他 Python 工具的設定。

簡化範例：

```toml
[project]
name = "pomelo-test"
version = "0.1.0"
requires-python = ">=3.8,<3.9"
dependencies = [
    "django",
    "oracledb",
]
```

`pyproject.toml` 描述的是允許範圍與專案需求，不是單一電腦目前安裝的結果。

### 10.3 `uv.lock`：鎖定完整依賴結果

本文件中的 lockfile（鎖定檔）就是 `uv.lock`。

它會記錄：

- 直接依賴。
- 間接依賴。
- uv 解析後採用的實際版本。
- 不同平台、Python 版本與環境條件下的依賴資訊。

`uv.lock` 應提交 Git，由 uv 管理，不建議手動編輯，也不應因同步失敗就直接刪除。

### 10.4 `.python-version`：指定專案 Python

`.python-version` 記錄專案偏好使用的 Python 版本，例如：

```text
3.8
```

它由以下指令建立：

```powershell
uv python pin 3.8
```

`.python-version` 應與 `pyproject.toml` 的 `requires-python` 相容。



### 10.5 重要檔案之間的關係

```text
requirements.txt
    │ 第一次遷移時匯入
    ▼
pyproject.toml
    │ 宣告 Python 範圍與直接依賴
    ▼
uv lock
    │ 解析直接與間接依賴
    ▼
uv.lock
    │ 保存實際鎖定結果
    ▼
uv sync
    │ 建立或同步本機環境
    ▼
.venv
    │ uv run 在此環境執行程式
    ▼
Python／Django
```

---

## 11. 理解 uv 的核心概念

### 11.1 Python 直譯器、套件與虛擬環境

- **Python 直譯器**：負責讀取並執行 Python 程式。
- **套件**：提供 Django、資料庫連線或其他額外功能。
- **虛擬環境**：隔離單一專案所使用的 Python 套件。
- **uv**：協助選擇 Python、建立虛擬環境、管理依賴並執行指令。

不同專案可能需要不同 Python 或套件版本，因此不應把所有套件都安裝到同一個全域 Python 環境。

### 11.2 直接依賴與間接依賴

直接依賴是專案主動使用的套件；間接依賴則是由直接依賴帶入的套件。

例如：

```text
專案
└── Django：直接依賴
    ├── asgiref：間接依賴
    └── sqlparse：間接依賴
```

`pyproject.toml` 主要宣告直接依賴；`uv.lock` 則保存 uv 解析後的完整直接與間接依賴結果。

### 11.3 宣告版本範圍與鎖定版本

`pyproject.toml` 可能宣告：

```toml
"django>=4.2,<5"
```

這代表專案允許使用 Django 4.2 以上、5 以下的版本。

`uv.lock` 則記錄 uv 在此範圍內實際選擇的版本。這使不同同事不需要各自重新選擇套件版本。

### 11.4 Lock 與 Sync

Lock 與 Sync 是兩個不同動作：

- **Lock**：根據 `pyproject.toml` 計算相容的依賴組合，並將結果寫入 `uv.lock`。
- **Sync**：依照 `uv.lock` 將套件安裝至 `.venv`，讓本機環境符合鎖定結果。

```text
pyproject.toml
      │
      │ Lock
      ▼
   uv.lock
      │
      │ Sync
      ▼
    .venv
```

### 11.5 `uv sync` 與 `uv sync --locked`

| 指令 | 行為 | 適用情境 |
|---|---|---|
| `uv sync` | 同步環境；必要時可能更新 `uv.lock` | 維護者調整依賴後 |
| `uv sync --locked` | 要求 `uv.lock` 必須一致，否則停止 | 同事還原環境、CI/CD |

推薦一般同事使用：

```powershell
uv sync --locked
```

---

## 12. 常用指令速查

| 目的 | 指令 |
|---|---|
| 查看 uv 版本 | `uv --version` |
| 最小初始化既有專案 | `uv init --bare --python 3.8` |
| 固定專案 Python | `uv python pin 3.8` |
| 匯入原有 requirements | `uv add -r requirements.txt` |
| 依既有 lockfile 還原環境 | `uv sync --locked` |
| 預覽環境同步差異 | `uv sync --locked --dry-run` |
| 執行 Django system check | `uv run --locked python manage.py check` |
| 啟動 Django | `uv run --locked python manage.py runserver` |
| 新增正式依賴 | `uv add <PACKAGE>` |
| 新增開發依賴 | `uv add --dev <PACKAGE>` |
| 移除依賴 | `uv remove <PACKAGE>` |
| 查看已安裝套件 | `uv pip list` |
| 查看依賴樹 | `uv tree` |
| 查看共用快取位置 | `uv cache dir` |
