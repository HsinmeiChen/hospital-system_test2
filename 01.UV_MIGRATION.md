# 使用 uv 遷移與管理 Python／Django 專案

本文件說明如何將既有、以 `requirements.txt` 管理套件的 Django 專案遷移至 uv。

指定使用：

```text
uv 0.10.12
Python 3.8.x
```

## 目錄

1. [進入 Django 專案根目錄](#1-進入-django-專案根目錄)
2. [安裝指定版本的 uv](#2-安裝指定版本的-uv)
3. [清除舊虛擬環境](#3-清除舊虛擬環境)
4. [初始化 uv 專案](#4-初始化-uv-專案)
5. [從 requirements.txt 匯入](#5-從-requirementstxt-匯入)
6. [驗證遷移結果](#6-驗證遷移結果)
7. [提交遷移結果](#7-提交遷移結果)
8. [遷移後](#8-遷移後)

---

## 1. 進入 Django 專案根目錄

請開啟 PowerShell，將 `<POMELO_TEST_PATH>` 替換成自己電腦中 `Pomelo_test` 資料夾的完整路徑：

```powershell
cd '<POMELO_TEST_PATH>'
```

例如：

```powershell
cd 'C:\Users\<USERNAME>\Desktop\hospital-system\Pomelo_test'
```


簡單確認目前目錄中存在 Django 與原有套件清單：

```powershell
Test-Path '.\manage.py'
Test-Path '.\requirements.txt'
```

預期結果皆為：

```text
True
```

後續指令都必須在此目錄中執行。

---

## 2. 安裝指定版本的 uv

本專案統一使用 uv `0.10.12`。請在 PowerShell 執行：

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/0.10.12/install.ps1 | iex"
```

安裝完成後，請重新開啟 PowerShell 或 VS Code Terminal，再確認版本：

```powershell
uv --version
```

預期結果：

```text
uv 0.10.12
```

若仍顯示找不到 `uv`，請先重新開啟終端機，再確認 uv 的安裝目錄是否已加入 Windows `PATH`。

> 本專案指定使用 uv `0.10.12`。請勿自行執行 `uv self update`，以免與團隊使用的版本不同。

---

## 3. 清除舊虛擬環境

確認目前位於 `Pomelo_test` 目錄後，移除舊的虛擬環境。若目錄不存在，PowerShell 會自動跳過：

```powershell
Remove-Item -Recurse -Force '.\.venv' -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force '.\venv' -ErrorAction SilentlyContinue
```

此步驟只會移除：

```text
.venv/
venv/
```


---

## 4. 初始化 uv 專案

一般的 `uv init` 是建立新應用程式的指令。視現有目錄內容及 uv 版本，它還可能建立 README、範例 Python 檔案及 Git 相關檔案。

若為既有 Django 專案，建議使用最小初始化：

```powershell
uv init --bare --python 3.8
uv python pin 3.8
```

執行後會產出以下兩個檔案：

```text
pyproject.toml
.python-version
```

其中：

- `uv init --bare --python 3.8`：建立最小的 `pyproject.toml`，並將 Python 需求設定為 3.8。
- `uv python pin 3.8`：建立 `.python-version`，讓 uv 在此專案中優先使用 Python 3.8。


> 如果 `pyproject.toml` 已經存在，`uv init` 會停止。此時應先確認專案是否已完成 uv 初始化，而不是刪除原有檔案後重新開始。

---

## 5. 從 `requirements.txt` 匯入

執行：

```powershell
uv add -r requirements.txt
```

此步驟會：
- 將 requirements.txt 中的依賴加入 pyproject.toml
- 解析依賴並產生 uv.lock
- 建立或同步 .venv
- 將套件安裝至 .venv

由於 `uv add -r requirements.txt` 已完成第一次環境同步，
不需要再額外執行 `uv sync`。

## 6. 驗證遷移結果

使用 uv 管理的環境執行 Django system check：

```powershell
uv run --locked python manage.py check
```

正常情況會顯示類似：

```text
System check identified no issues
```

用來確認 Django 專案設定、已安裝 App、模型與部分系統組態是否可正常載入。

### 遷移完成後確認

確認以下檔案已存在：

pyproject.toml
uv.lock
.python-version
.venv/

其中：

pyproject.toml：記錄專案需求
uv.lock：記錄專案實際使用的套件版本
.python-version：指定專案 Python 版本
.venv/：本機虛擬環境，不提交 Git

---

## 7. 提交遷移結果

確認 `.gitignore` 包含：

.venv/
venv/
.env

### 應提交 Git：

pyproject.toml
uv.lock
.python-version

### 不應提交：

.venv/
venv/
.env

`requirements.txt` 是否保留在 Git 中，應等確認部署與其他既有流程後再決定。遷移當下不要直接刪除。

---

## 8. 遷移後

完成本文件後，專案已由原本的：

```text
requirements.txt
+
pip
+
venv
```

轉換為：

```text
pyproject.toml
+
uv.lock
+
.venv
```

- Clone 專案與建立本機環境：請參閱 `02.UV_PROJECT_SETUP.md`
- 日常 uv 使用方式：請參閱 `00.UV_USAGE_GUIDE.md`
- 套件整理與 Python／Django 升級：請參閱 `03.UV_PROJECT_UPGRADE.md`