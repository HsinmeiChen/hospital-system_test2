# 本地端測試啟動指南 (使用虛擬環境)

本文件旨在協助開發者在本地端建立獨立的 Python 虛擬環境，並整合生產環境的資料 (`db.sqlite3` 與靜態檔案)，以啟動完整的測試環境。

## 前置準備

1.  **資料庫檔案 (`db.sqlite3`)**：
    *   從生產環境複製 `db.sqlite3`。
    *   放置位置：`Pomelo_test/` 目錄下（與 `manage.py` 同一層級）。

2.  **媒體檔案 (`media`)**：
    *   從生產環境複製整個 `media` 資料夾。
    *   放置位置：`Pomelo_test/media/`。
    *   **重要**：資料庫中的圖片欄位僅儲存路徑，若缺少此資料夾，網站圖片將無法顯示。
    *   **[異動]** 原本在 `Public/video/` 的影片，現在應複製到 `Pomelo_test/media/video/`。

3.  **靜態檔案 (`Public`)**：
    *   確認 `Pomelo_test/Public/common/` 資料夾存在。
    *   **重要**：為了優化版控效能，以下大型資料夾已加入 `.gitignore`，請依照以下路徑對應關係手動複製：
        *   **圖片**：從 原本的 `Public/img/`  --> 複製到 `Pomelo_test/Public/common/img/`
        *   **文件**：從 原本的 `Public/doc/`  --> 複製到 `Pomelo_test/Public/common/doc/`
    *   **注意**：`video/` 資料夾已改為放置於 `Pomelo_test/media/video/`，請勿放回 Public。
    *   其餘程式相關靜態檔 (如 `css/`, `js/`) 應已隨 Git 同步。
        *   若專案結構有異動，請確保 `settings.py` 中的 `STATICFILES_DIRS` 指向正確位置。

4.  **環境變數 (`.env`)**：
    *   確保 `Pomelo_test/` 目錄下已有 `.env` 檔案，且內容設定正確 (參考 `env.example`)。

5. **Python 版本管理 (pyenv) (必要)**：
    *   本專案使用 Python 3.8.10，建議使用 pyenv 管理 Python 版本以確保環境一致性。
    *   **安裝步驟**（PowerShell，以管理員身分執行，可在任何目錄執行）：
        ```powershell
        # 1. 下載並安裝 pyenv-win（會自動安裝到 %USERPROFILE%\.pyenv）
        git clone https://github.com/pyenv-win/pyenv-win.git $env:USERPROFILE\.pyenv
        
        # 2. 設定環境變數（一次性設定）
        [System.Environment]::SetEnvironmentVariable('PYENV',$env:USERPROFILE + "\.pyenv",[System.EnvironmentVariableTarget]::User)
        [System.Environment]::SetEnvironmentVariable('PYENV_ROOT',$env:USERPROFILE + "\.pyenv",[System.EnvironmentVariableTarget]::User)
        [System.Environment]::SetEnvironmentVariable('PYENV_HOME',$env:USERPROFILE + "\.pyenv",[System.EnvironmentVariableTarget]::User)
        [System.Environment]::SetEnvironmentVariable('path', $env:USERPROFILE + "\.pyenv\pyenv-win\bin;" + $env:USERPROFILE + "\.pyenv\pyenv-win\shims;" + [System.Environment]::GetEnvironmentVariable('path', [System.EnvironmentVariableTarget]::User),[System.EnvironmentVariableTarget]::User)
        ```
    *   **使用步驟**（重新啟動終端機後執行）：
        ```bash
        # 安裝 Python 3.8.10（僅需執行一次）
        pyenv install 3.8.10
        
        # 進入專案目錄後，pyenv 會自動使用 .python-version 指定的版本
        cd Pomelo_test
        python --version  # 應顯示 Python 3.8.10
        ```

6. **Oracle Client (必要)**：
    *   本專案連線舊版 Oracle 資料庫需使用 Thick Mode。
    *   請下載 [Oracle Instant Client (Basic Package)](https://www.oracle.com/database/technologies/instant-client/winx64-64-downloads.html)。
    *   解壓縮至 `C:\oracle\instantclient_11_2_x64`。
    *   將該路徑加入 Windows 系統環境變數 `Path` 中。
---

## 👩‍💻 開發規範與默契 (Development Guidelines)

### 1. 專案結構與 Templates
*   **集中管理**：所有 HTML Template 檔案皆位於 `Pomelo_test/templates/` 目錄下。
*   **設定對應**：`settings.py` 中的 `TEMPLATES` 設定已指向此目錄，請勿隨意修改。

### 2. 靜態資源 (Static Files)
*   **分類原則**：
    *   **Common**：全站共用的 CSS/JS/Img 請放於 `Public/common/`。
    *   **Apps**：各個 App 專屬的資源請放於 `Public/apps/[AppName]/`。
### 3. Manifest 與 Static Files Storage
*   **設定**：`settings.py` 中使用了 `whitenoise.storage.CompressedManifestStaticFilesStorage`。
*   **影響**：此設定會對靜態檔案進行雜湊 (Hash) 處理。因此，**所有** 靜態資源 (含 `manifest.webapp`, CSS, JS) 都 **必須** 使用 Django `{% static '...' %}` 標籤，無法直接寫死路徑。
*   **範例**：
    ```html
    <script src="{% static 'common/js/slick.js' %}"></script>
    <link rel="stylesheet" href="{% static 'common/css/slick.css' %}" />
    ```

---

## 🚀 第一次執行環境建置 (Initial Setup)

若您是初次拉取專案或重新建置環境，請完整執行以下步驟：

### 1. 切換至專案目錄

所有操作皆需在 `Pomelo_test` 資料夾下執行。

```bash
cd Pomelo_test
```

### 2. 確認 Python 版本

專案根目錄已包含 `.python-version` 檔案，進入專案目錄後 pyenv 會自動切換至 Python 3.8.10。

```bash
python --version  # 應顯示 Python 3.8.10
```

### 3. 建立虛擬環境 (Virtual Environment)

使用虛擬環境可確保套件版本獨立，不影響系統其他專案。

```bash
# 建立虛擬環境 (名稱為 venv)
python -m venv venv

# 啟動虛擬環境
venv\Scripts\activate
```
*(啟動成功後，終端機提示字元前會出現 `(venv)` 字樣)*

### 4. 安裝依賴套件

在虛擬環境中安裝專案所需的所有 Python 套件。

> 💡 **提示**：若安裝過程出現編碼錯誤或版本解析失敗，建議先升級 pip：
> ```bash
> python -m pip install --upgrade pip
> ```

```bash
pip install -r requirements.txt
```

### 5. 執行資料庫遷移 (Migrate)

將本地程式碼 (Models) 的變更應用到資料庫，確保結構一致。

```bash
python manage.py migrate
```

### 6. 建立超級使用者 (可選)

若需登入 Django Admin 且不知道原資料庫密碼，請建立新帳號。

```bash
python manage.py createsuperuser
```

### 7. 啟動開發伺服器

```bash
python manage.py runserver
```

---

## 🔄 第二次之後的啟動 (Daily Usage)

若環境已建置完成，只需執行以下步驟即可快速啟動：

### 1. 切換至專案目錄並啟動虛擬環境

```bash
cd Pomelo_test
venv\Scripts\activate
```

### 2. 啟動伺服器

```bash
python manage.py runserver
```

---

## 驗證與除錯

1.  **瀏覽網站**：
    前往 [http://127.0.0.1:8000/](http://127.0.0.1:8000/)。

2.  **檢查資源載入**：
    *   若 CSS/JS 遺失，請檢查 `Pomelo_test/Public` 目錄。
    *   若圖片破圖，請檢查 `Pomelo_test/media` 目錄權限與內容。

3.  **離開虛擬環境**：
    測試結束後，可輸入指令退出：
    ```bash
    deactivate
    ```

---

## 🛠️ 進階：本地 Nginx + Django 整合測試 (模擬正式站)

若需要模擬正式站的靜態檔案處理機制 (`collectstatic`)，請使用以下批次檔啟動。

### 1. 安裝與操作 Nginx
*   **下載**：前往 [Nginx for Windows](https://nginx.org/en/download.html) 下載並解壓縮。
*   **路徑**：建議解壓至短路徑，例如 `C:\nginx-1.24.0\` (避免路徑含中文或空白)。
*   **常用指令** (需以管理員身分執行 CMD)：
    ```bash
    cd C:\nginx-1.24.0

    # 啟動 Nginx (視窗閃退為正常，請至工作管理員確認 nginx.exe 是否執行)
    start nginx

    # 重新讀取設定 (修改 nginx.conf 後執行)
    nginx -s reload

    # 停止 Nginx
    nginx -s stop
    ```

### 2. 設定 Nginx (`conf/nginx.conf`)
請修改 `conf/nginx.conf` 內的 `server` 區塊，以設定靜態檔路徑與 Django 反向代理。

```nginx
server {
    listen       80;
    server_name  localhost;

    # 設定靜態檔案路徑 (Alias 必須使用正斜線 /)
    # 請修改為您本機的實際路徑
    location /Public/ {
        alias "C:/path/to/your/project/Pomelo_test/Public/";
    }
    
    # 設定媒體檔案路徑
    location /media/ {
        alias "C:/path/to/your/project/Pomelo_test/media/";
    }

    # 反向代理至 Django (Runserver Port 3611)
    location / {
        proxy_pass http://127.0.0.1:3611;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 3. 設定批次檔 (Batch Files)

請建立以下兩個批次檔，並**依照您的電腦環境修改路徑變數**。

#### A. `P006.bat` (啟動腳本)
負責啟動 Nginx、虛擬環境、執行資料庫維護與收集靜態檔。

**需修改變數：**
*   `NGINX_DIR`: 您的 Nginx 資料夾路徑
*   `VENV_DIR`: 您的虛擬環境 `Scripts` 資料夾路徑
*   `PROJECT_DIR`: 您的專案 `Pomelo_test` 資料夾路徑

```batch
@echo off
title P006_Server - Startup Sequence

:: --- 請修改以下路徑 ---
set "NGINX_DIR=C:\path\to\nginx-1.24.0"
set "VENV_DIR=C:\path\to\project\venv\Scripts"
set "PROJECT_DIR=C:\path\to\project\Pomelo_test"
set "PORT=3611"

echo [STEP 1] Starting Nginx...
cd /d "%NGINX_DIR%"
nginx -s stop >nul 2>&1
start nginx

echo [STEP 2] Activating Virtual Environment...
cd /d "%VENV_DIR%"
call activate.bat

echo [STEP 3] Database Maintenance...
cd /d "%PROJECT_DIR%"
python manage.py clearsessions
python manage.py shell -c "from django.db import connection; cursor = connection.cursor(); cursor.execute('VACUUM');"

echo [STEP 3.5] Collecting Static Files...
python manage.py collectstatic --noinput

echo [STEP 4] Starting Django Server on Port %PORT%...
python manage.py runserver %PORT%
```

#### B. `re_Python.bat` (重啟腳本)
負責安全停止所有服務並重新啟動。

**需修改變數：**
*   `NGINX_DIR`: 同上
*   `NEXT_SCRIPT`: 指向您的 `P006.bat` 檔名

```batch
@echo off
title re_Python - Restart Sequence

set "NGINX_DIR=C:\path\to\nginx-1.24.0"
set "NEXT_SCRIPT=P006.bat"

echo [STEP 1] Stopping Nginx...
cd /d "%NGINX_DIR%"
nginx -s stop >nul 2>&1
taskkill /f /im nginx.exe >nul 2>&1

echo [STEP 2] Stopping Python...
taskkill /f /t /im Python.exe >nul 2>&1

echo [STEP 3] Launching Startup Script...
cd /d "%~dp0"
start "P006_Server" cmd /k call "%NEXT_SCRIPT%"
exit
```

### 4. 執行測試
雙擊執行 `re_Python.bat` 即可自動完成重啟流程。
前往 [http://127.0.0.1/](http://127.0.0.1/) (預設 Port 80，由 Nginx 服務) 瀏覽。
